export enum StoreStatus {
  OPEN = "OPEN",
  CLOSED = "CLOSED",
}

export enum ProductUnit {
  PCS = "PCS",
  KG = "KG",
}

export enum ProductFilter {
  LOW_STOCK = "LOW_STOCK",
  NO_STOCK = "NO_STOCK",
}
